# Secrets consumed by the CDK app (packages/cdk).
# Copy this file to .env at the project root and fill in values.
# .env is gitignored; do NOT commit real values.

# Tavily Web Search API key (https://tavily.com/).
# Used by the /web-search Lambda when searchEngine is "Tavily".
TAVILY_API_KEY=
